name: flutter_app
description: Global Photo Manager Flutter client
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3
  shared_preferences: ^2.0.7
  provider: ^6.0.0
  image_picker: ^0.8.2
  fl_chart: ^0.36.2
  connectivity_plus: ^2.0.2
  hive: ^2.0.4
  hive_flutter: ^1.1.0
  path_provider: ^2.0.2
  mime: ^1.0.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^1.1.0
  build_runner: ^2.0.6

flutter:
  uses-material-design: true
